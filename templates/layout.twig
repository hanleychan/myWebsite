<!DOCTYPE html>
<html lang="en">
<head>
    <title>Hanley Chan | Web Developer</title>
    <meta charset="utf-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="Hanley Chan is a PHP Web Developer from Vancouver, BC.">
    <link rel="icon" href="images/favicon.ico">
    <link rel="stylesheet" href="stylesheets/normalize.css">
    <link href='https://fonts.googleapis.com/css?family=Montserrat' rel='stylesheet' type='text/css'>
    <link rel="stylesheet" href="{{ path_for('portfolio') }}stylesheets/main.css">
    <link rel="stylesheet" href="{{ path_for('portfolio') }}stylesheets/responsive.css">
</head>
<body>
    <div id="container">
        <header>
            <nav>
                <div>
                    <h1><a href={{ path_for('portfolio') }}>Hanley Chan</a></h1>

                    <ul id="navigationMenu">
                        <li{% if page == 'portfolio' %} id="selectedPage"{% endif %}><a href="{{ path_for('portfolio') }}">Home</a></li>
                        <li{% if page == 'about' %} id="selectedPage"{% endif %}><a href="{{ path_for('about') }}">About</a></li>
                        <li{% if page == 'contact' %} id="selectedPage"{% endif %}><a href="{{ path_for('contact') }}">Contact</a></li>
                    </ul>
                </div>
            </nav>
        </header>

        <div id="wrapper">
            <section>
                {% block content %}
                {% endblock %}
            </section>
        </div>
    </div>

    <footer>
        <p>&copy;{{ "now"|date("Y") }} Hanley Chan</p>
    </footer>

    {% block scripts %}
    {% endblock %}
</body>
</html>
